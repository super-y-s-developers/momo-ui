import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import { withKnobs, boolean } from "@storybook/addon-knobs";
import Input from "./Input";

<Meta title="Atoms/Input" component={Input} />

# Inputs

The Inputs of Momo UI are very practical. You won't find a `select` or `text-area` component. All of these variants are included in this component.

## Usage

Inputs are used to get information from the user. You can find them inside a form, a search bar, and basically anywhere.

## Text Input

Text Inputs can be used used for names, short prases or similar.

<Preview withToolbar>
  <Story name="text">
    <Input
      disabled={boolean("Disabled", false)}
      type="text"
      placeholder="Hello World"
    />
  </Story>
</Preview>
